# RepSnack Cloudflare Pages Redirects
# Note: Cloudflare Pages natively serves extensionless URLs (e.g. /terms -> terms.html)
# and redirects .html paths to extensionless counterparts.
# Do not add 200 rewrites to .html files as that creates an infinite 308 loop.
